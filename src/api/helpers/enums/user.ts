export enum UserRole {
  CUSTOMER = "customer",
  ADMIN = "admin",
  SUPERADMIN = "superadmin",
}

/* export enum UserType {
  ADMIN = "admin",
  CUSTOMER = "customer",
} */

export enum GenderType {
  MALE = "male",
  FEMALE = "female",
}
